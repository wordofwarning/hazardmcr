<div class="programme">
	{% for slot in site.schedule %}
		<div class="hour">
			<h4>{{ slot.label }}</h4>
			{% for artist in slot.artists %}
				<a href="{{ page.rootpath }}{{ artist.folder }}/index.html">{{ artist.name }}</a>
			{% endfor %}
		</div>
	{% endfor %}
</div>