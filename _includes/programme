<div class="programme">
	<div class="hour">
		<h4><em>What's on</em>: All afternoon</h4>
		{% for link in site.allday %}
			<a href="{{ page.rootpath }}{{ link.folder }}/index.html">{{ link.name }}</a>
		{% endfor %}
	</div>

	<div class="hour">
		<h4>12pm</h4>
		{% for link in site.12pm %}
			<a href="{{ page.rootpath }}{{ link.folder }}/index.html">{{ link.name }}</a>
		{% endfor %}
	</div>

	<div class="hour">
		<h4>1pm</h4>
		{% for link in site.1pm %}
			<a href="{{ page.rootpath }}{{ link.folder }}/index.html">{{ link.name }}</a>
		{% endfor %}
	</div>

	<div class="hour">
		<h4>2pm</h4>
		{% for link in site.2pm %}
			<a href="{{ page.rootpath }}{{ link.folder }}/index.html">{{ link.name }}</a>
		{% endfor %}
	</div>

	<div class="hour">
		<h4>3pm</h4>
		{% for link in site.3pm %}
			<a href="{{ page.rootpath }}{{ link.folder }}/index.html">{{ link.name }}</a>
		{% endfor %}
	</div>

	<div class="hour">
		<h4>4pm</h4>
		{% for link in site.4pm %}
			<a href="{{ page.rootpath }}{{ link.folder }}/index.html">{{ link.name }}</a>
		{% endfor %}
	</div>
	<!--
	<div class="hour">
		<h4>5pm</h4>
		{% for link in site.5pm %}
			<a href="{{ page.rootpath }}{{ link.folder }}/index.html">{{ link.name }}</a>
		{% endfor %}
	</div>
	-->
</div>